package asm.homecataloguer.helpers;

import java.util.ArrayList;
import java.util.Date;

import asm.homecataloguer.models.CatalogItem;
import asm.homecataloguer.models.ContentType;

public class UserDBHelper
{
	/*public static void main(String[] args)
	{
		System.out.println("Starting");
		
		CatalogDBHelper catalogDBHelper = new CatalogDBHelper();
		for (CatalogItem item : catalogDBHelper.loadAll())
		{
			System.out.println(item.getTitle());
		}
		
		CatalogItem newItem = new CatalogItem(1, 2, 32, 98, ContentType.VIDEO, "Title 2", new Date(), new byte[3]);
		catalogDBHelper.saveItem(newItem, null);
		
		for (CatalogItem item : catalogDBHelper.loadAll())
		{
			System.out.println(item.getTitle());
		}
	}*/
}
